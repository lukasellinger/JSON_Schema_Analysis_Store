{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "description": "details of a single test",
  "properties": {
    "success": {
      "type": "boolean",
      "description": "True if all the tests passed correctly, otherwise false due to an api test or multiple api test failures"
    },
    "message": {
      "type": "string",
      "description": "Details about the series of api tests completed. Also includes any exceptions"
    },
    "tests": {
      "$ref": "#/definitions/apiTest.json"
    },
    "runTime": {
      "type": "integer",
      "description": "Duration taken to run all the tests, displayed in seconds"
    }
  },
  "definitions": {
    "apiTest.json": {
      "$schema": "http://json-schema.org/draft-04/schema#",
      "definitions": {
        "apiTest": {
          "type": "object",
          "properties": {
            "api": {
              "type": "string",
              "description": "URL called to perform the api test"
            },
            "message": {
              "type": "string",
              "description": "Describes the outcome of the api test. Includes any exceptions if required"
            },
            "success": {
              "type": "boolean",
              "description": "true if the api test succeeds or false if the api test fails"
            }
          },
          "additionalProperties": false,
          "required": [
            "api",
            "message",
            "success"
          ]
        }
      },
      "type": "array",
      "items": {
        "$ref": "#/definitions/apiTest.json_definitions_apiTest"
      }
    },
    "apiTest.json_definitions_apiTest": {
      "type": "object",
      "properties": {
        "api": {
          "type": "string",
          "description": "URL called to perform the api test"
        },
        "message": {
          "type": "string",
          "description": "Describes the outcome of the api test. Includes any exceptions if required"
        },
        "success": {
          "type": "boolean",
          "description": "true if the api test succeeds or false if the api test fails"
        }
      },
      "additionalProperties": false,
      "required": [
        "api",
        "message",
        "success"
      ]
    }
  }
}